<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET 은 webviewlib 에서도 병합되지만, 앱 의도를 명시적으로 드러내기 위해 함께 선언 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 네트워크 상태 확인(오프라인 처리 등)에 사용 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- getUserMedia(카메라) — 웹에서 카메라 사용 시 필요 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 마이크 — 웹에서 음성 입력 사용 시 필요 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 위치 — geolocation 사용 시 필요 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 다운로드 저장 — Android 9(API 28) 이하에서만 필요. 10+ 는 scoped storage 로 불필요 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 카메라가 없는 기기에서도 설치 가능하도록 required=false -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.SmWebview">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
